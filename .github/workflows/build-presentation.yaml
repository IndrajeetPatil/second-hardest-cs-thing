name: Build and Deploy Presentation

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

jobs:
  build-and-deploy:
    # Integration trial for workflows#61; return to @main after it merges.
    uses: IndrajeetPatil/workflows/.github/workflows/build-presentation-python.yaml@feat/revealjs-a11y-extension
    permissions:
      contents: read
      pages: write
      id-token: write
